[build-system]
requires = ["scikit-build-core", "pybind11"]
build-backend = "scikit_build_core.build"

[project]
name = "pydeform"
version = "0.5.2"
description = ""
readme = "README.md"
authors = [
    { name = "Simon Ekström" },
]
dependencies = [
    "numpy",
    "python-stk",
    "PyYAML",
]
